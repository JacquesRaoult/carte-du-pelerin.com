<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background: #f5f5f5;
        }

        .header {
            background: #5a67d8;
            color: white;
            padding: 20px 40px;
            margin: -20px -20px 20px -20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .header h1 {
            margin: 0;
            font-size: 24px;
        }

        .header-buttons {
            display: flex;
            gap: 12px;
            align-items: center;
        }

        .logout {
            background: white;
            color: #5a67d8;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 6px;
            font-weight: 500;
            transition: background 0.2s;
        }

        .logout:hover {
            background: #f0f0f0;
        }

        .content {
            background: white;
            padding: 20px;
            border-radius: 8px;
            max-width: 1200px;
            margin: 0 auto;
        }

        .top-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .count-text {
            color: grey;
        }

        .btn {
            padding: 8px 16px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 14px;
            text-decoration: none;
            display: inline-block;
        }

        .btn-primary {
            background: #48bb78;
            color: white;
        }

        .btn-edit {
            background: #4299e1;
            color: white;
            margin-right: 5px;
        }

        .btn-delete {
            background: #f56565;
            color: white;
        }

        .btn:hover {
            opacity: 0.8;
        }

        .btn:disabled {
            opacity: 0.5;
            cursor: not-allowed;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        th,
        td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        th {
            background: #f8f9fa;
            font-weight: bold;
        }

        tr:hover {
            background: #f8f9fa;
        }

        .badge {
            background: #5a67d8;
            color: white;
            padding: 4px 8px;
            border-radius: 4px;
            font-size: 12px;
        }

        .actions {
            white-space: nowrap;
        }

        @media (max-width: 768px) {
            .header {
                flex-direction: column;
                gap: 15px;
                text-align: center;
            }

            .header-buttons {
                width: 100%;
                justify-content: center;
            }
        }
    </style>
</head>

<body>
    <div class="header">
        <h1>⚙ Console d'administration</h1>
        <div class="header-buttons">
            <a href="/" class="logout">🗺️ Carte</a>
            <a href="/admin/logout" class="logout">🚪 Déconnexion</a>
        </div>
    </div>

    <div class="content">
        <div class="top-bar">
            <h2>📍 <span class="count-text">
                    <%= it.sites ? it.sites.length : 0 %>
                </span> lieux répertoriés dans la base de données</h2>
            <button class="btn btn-primary" disabled>➕ Ajouter un lieu</button>
        </div>

        <% if (it.sites && it.sites.length> 0) { %>
            <table>
                <thead>
                    <tr>
                        <th>Nom</th>
                        <th>Catégorie</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% it.sites.forEach(function(site) { %>
                        <tr>
                            <td>
                                <%= site.name %>
                            </td>
                            <td><span class="badge">
                                    <%= site.category %>
                                </span></td>
                            <td class="actions">
                                <button class="btn btn-edit" disabled>✏️ Éditer</button>
                                <button class="btn btn-delete" disabled>🗑️ Supprimer</button>
                            </td>
                        </tr>
                        <% }) %>
                </tbody>
            </table>
            <% } else { %>
                <p>Aucun lieu trouvé.</p>
                <% } %>
    </div>
</body>

</html>